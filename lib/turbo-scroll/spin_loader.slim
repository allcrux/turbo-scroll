css:
  .turbo_loader {
    display: inline-block;
    position: relative;
    width: 45px;
    height: 45px;
    margin: auto;
  }
  .turbo_loader div {
    box-sizing: border-box;
    display: block;
    position: absolute;
    width: 32px;
    height: 32px;
    margin: 8px 0px;
    border: 3px solid #00afd4;
    border-radius: 50%;
    animation: turbo_loader 1.2s cubic-bezier(0.5, 0, 0.5, 1) infinite;
    border-color: #00afd4 transparent transparent transparent;
  }
  .turbo_loader div:nth-child(1) {
    animation-delay: -0.45s;
  }
  .turbo_loader div:nth-child(2) {
    animation-delay: -0.3s;
  }
  .turbo_loader div:nth-child(3) {
    animation-delay: -0.15s;
  }
  .page-loading {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
  }

  @keyframes turbo_loader {
    0% {
      transform: rotate(0deg);
    }
    100% {
      transform: rotate(360deg);
    }
  }

.turbo_loader
  div
  div
  div
  div
